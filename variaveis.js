// VARIÁVEIS

let idade = 26 //criando a primeira variável

console.log(idade, typeof idade)

// Tipos de variáveis

idade= 30;                   // Number
let numero = 30;            // Number
let altura = 1.80;         // Number
let nome = "Soares"       // String (texto)
let  estudando = true    // Booleano (true ou false)

console.log (idade, typeof idade)
console.log (numero, typeof numero)
console.log (altura, typeof altura)
console.log (nome, typeof nome)
console.log (estudando, typeof estudando)

let outra;    //sem definiar valores a vaiável é determinada como undefined.
console.log(outra)

let nula = null;
console.log(nula)

const constante = 100;  //para variáveis constantes, porém, não pode ser alterada.
console.log(constante)

